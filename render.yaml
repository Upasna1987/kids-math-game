services:
  - type: web
    name: kids-math-game
    env: node
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /test
    autoDeploy: true 